import React, { PropTypes } from "react"

export default function ReportingEvents(props, context) {
    console.log('debug: props ', props);
    return (
        <div>
            Здесь будут отчётные события
        </div>
    )
}